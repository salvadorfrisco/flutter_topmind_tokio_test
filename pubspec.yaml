name: flutter_topmind_tokio_test
description: "Projeto para aplicação de teste para a empresa TopMind."
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  firebase_core: ^3.6.0
  firebase_auth: ^5.3.3
  shared_preferences: ^2.3.3
  flutter_svg: ^2.0.10+1
  font_awesome_flutter: ^10.8.0
  provider: ^6.1.2
  url_launcher: ^6.2.5
  flutter_dotenv: ^5.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
